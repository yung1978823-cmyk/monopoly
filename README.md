# 大富翁 · 每日棋盤

這一版只做每日那一層：一塊自己的棋盤、四個地標、骰子、分數，以及一位內建對手可以來砸地標。

沒有公開桌、沒有賽季獎、沒有領地、沒有鏈上錢包、沒有 Solana。

## 這一層怎麼玩

- 沒有 NFT 也可以走。分數不能換成 DST，也不計入賽季。
- 骰子不出售。每 30 分鐘補 1 顆，最多存 20 顆。
- 「補一粒（測試）」只為了不用等就能走完一輪。正式補充仍是 30 分鐘。
- 擲 1 顆（1–6），只在自己的小圈上走，得到的是分數。
- 走到空著或已成廢墟的地標，可以蓋起來。防守等於已建成的地標數，0 至 4。
- 攻擊花 1 顆。點數低過防守拿 0 DST；等於拿 1 DST；高過拿 min(5, max(2, 點數 − 防守 + 1))，且不超過防守方剩餘錢包，也不超過當日 5 DST。
- 「我有 NFT」是試作開關。關掉時 DST 進出是 0。打開時可以放出最多 5 DST，對手阿強是帶 5 DST 錢包的 NFT 防守方。
- 阿強在這台裝置上，不連線。

棋盤會記在這台瀏覽器裡。沒有帳號，也沒有資料庫。

## 本地執行

需要 Node.js 20 或以上。

```bash
npm install
npm run dev
```

瀏覽器打開 [http://127.0.0.1:43123](http://127.0.0.1:43123)。

`npm run dev` 會聽 `0.0.0.0:43123`。

## 測試

```bash
npm test
```

DST 算式和骰子補充在 `lib/rules.ts`。棋盤怎麼走在 `lib/game.ts`。
